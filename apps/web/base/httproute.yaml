apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: web
  labels:
    app.kubernetes.io/name: web
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: north-south
      namespace: kgateway-system
  hostnames:
    - another-long-tenant-name-web.playground.sonia-certs.uk
  rules:
    - backendRefs:
        - name: web
          port: 80
